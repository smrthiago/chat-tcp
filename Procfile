# Railway usa a variável PORT automaticamente.
# Nosso servidor lê SERVER_PORT por padrão, mas Railway injeta PORT.
# Este Procfile garante compatibilidade: passa PORT como SERVER_PORT.
web: python -m src.server.server
